{
  "name": "syncoria-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker-compose up",
    "dev:web": "docker-compose up web",
    "dev:backend": "docker-compose up backend",
    "dev:livekit": "docker-compose up livekit",
    "build": "docker-compose build",
    "stop": "docker-compose down",
    "clean": "docker-compose down -v",
    "logs": "docker-compose logs -f",
    "install:all": "pnpm install && pnpm -r install"
  },
  "devDependencies": {
    "pnpm": "^10.4.1"
  }
}